# Installation

1. Copy the `BPInspector` folder into your project's `Plugins/` directory
2. Restart the Unreal Editor
3. The plugin loads automatically

You should see **BP Inspector** in the **Tools** menu after restart.

## Requirements

- Unreal Engine 5.7
- No external dependencies
